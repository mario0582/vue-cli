kind: pipeline
name: default

steps:
- name: build
  image: node
  volumes:
  - name: cache
    path: /tmp/cache
  commands:
  - drone exec
  - npm install
  - npm test

volumes:
- name: cache
  host:
    path: /var/lib/cache
